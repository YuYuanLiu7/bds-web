你現在是一位資深的全端 Tech Lead。請讀取文件。這是我們要開發的商用級線上課程系統的 PRD。


# BDS 線上課程系統：最終架構與企業風險評估報告

## 第一部分：平台規模與「倒閉/斷線」風險分析

使用雲端服務（SaaS）組合網站，最大的好處是成本極低，但最怕的就是「供應商突然消失」。以下為您盤點這四家公司的背景，以及萬一他們倒閉，您的「逃生計畫」是什麼：

### 1. 網頁代管：Netlify

- **公司規模（極大）：** 成立於 2014 年，是全球「現代化前端代管」的兩大巨頭之一（另一家是 Vercel）。背後有頂級創投 Andreessen Horowitz (a16z) 投資，估值超過 20 億美元（約 640 億台幣），客戶包含諸多跨國企業，倒閉機率極低。
- **如果倒閉的影響與對策：【影響極低，30 分鐘內可復原】**
    - 您的網站程式碼是安全的（存在微軟的 GitHub 裡）。
    - 如果 Netlify 突然倒了，您只要登入 Vercel 或 Cloudflare，重新綁定您的 GitHub 帳號，按一下部署（Deploy），網站半小時內就能在別的平台重新上線，學員甚至感覺不到差異。

### 2. 資料庫：Supabase

- **公司規模（極大/獨角獸）：** 被譽為「開源界的 Firebase」，在 2026 年中剛完成 5 億美元融資，公司估值高達 105 億美元（約 3,300 億台幣），背後投資人包含 Y Combinator、Stripe 與 Salesforce。資金極度充沛，極為穩定。
- **如果倒閉的影響與對策：【影響致命，但可透過備份完美防禦】**
    - 資料庫存著您所有的客戶名單和交易紀錄，一旦消失是毀滅性的。
    - **逃生對策：** 因為我們有設定 **「GitHub Actions 每天凌晨自動備份 .sql 檔到 Google Drive」**，這就是您的保命符。Supabase 的底層是標準的 PostgreSQL，如果平台真的不見了，您拿著這包備份檔，可以輕易去 AWS、Google Cloud 甚至您自己的 NAS 上，一鍵還原所有資料。

### 3. 影片代管：Bunny.net

- **公司規模（中大型且高度獲利）：** 成立於 2015 年的歐洲公司。與其他燒錢的新創不同，Bunny.net 以極致的性價比打下市場，目前服務全球超過 85,000 名企業客戶，擁有 119 個全球節點。因為其商業模式健康，營運非常穩健。
- **如果倒閉的影響與對策：【影響中等，需花幾天搬家】**
    - 網站上的影片會突然無法播放。
    - **逃生對策：** 只要您自己手邊（電腦或外接硬碟）有留存「原始 MP4 影片檔」，您只需要去申請 Cloudflare Stream 或 Amazon S3，把影片重新上傳，然後去資料庫把影片的 ID 換掉即可。雖然需要花幾天搬運檔案，但不會造成無可挽回的商業損失。

### 4. 寄信服務：Resend

- **公司規模（知名新創）：** 成立於 2022 年，是這四家中最年輕的。但它是由矽谷最著名的孵化器 Y Combinator 培育，並獲得 Vercel 與 Figma 創辦人的巨額投資，目前是前端工程師圈最紅的發信工具。
- **如果倒閉的影響與對策：【影響極微，10 分鐘內可替換】**
    - 系統會暫時發不出「購買成功」或「忘記密碼」的信件。
    - **逃生對策：** 寄信 API 是最容易被替換的零件。您只要去註冊老牌的 SendGrid 或 Mailgun，把程式碼裡的「API 金鑰」換掉，系統馬上就能重新開始寄信。

## 第二部分：系統各項費用、流量與限制總整理 (極致成本決議版)

### 1. 前端與 API 代管：Netlify Starter (免費版)

- **年費： NT$ 0 (完全合法商用)**
- **流量與限制：**
    - **流量上限：** 每月提供 100 GB 的網頁頻寬。因為最吃流量的影片已放置於 Bunny.net，100GB 的純網頁流量（文字與小圖）對於初期到中期的課程網站來說非常充足。
    - **編譯時間限制：** 每月提供 300 分鐘的 Build Minutes。每次更新程式碼上線約需 2-3 分鐘，若開發期頻繁更新需注意額度消耗；正式上線後，若只是新增課程或文章，不會消耗此額度（資料是從 Supabase 動態抓取）。
    - **API 限制：** 每月提供 125,000 次 API 呼叫，且單次執行不可超過 10 秒。這對於產生 Token、會員登入與金流串接來說已非常夠用。
    - **合法商用：** 條款允許放置自有商業產品與金流（如賣自己的線上課程），無需擔心像 Vercel 一樣被強制下線。

### 2. 影片串流與防盜：Bunny.net (按量計費)

- **年費： 約 NT$ 1,152** (以每月 500GB 流量 + 50GB 儲存空間估算，約 $3 USD / 月)
- **流量與限制：**
    - 無硬性限制，用多少算多少。
    - 串流流量費極低（全球均價 $0.005 USD / GB），影片儲存費為 $0.01 USD / GB / 月。
    - 500GB 流量非常充裕。若以 2.5GB 的高畫質 2 小時回放影片計算，每個月可以被完整看完整整 200 次；如果是分散看短影片，能服務的學員人數會更多。
    - 自帶 Edge HLS 加密與 Token 驗證機制，安全防盜。

### 3. 資料庫與會員系統：Supabase (免費版 + 喚醒優化)

- **年費： NT$ 0** (Free Tier)
- **流量與限制：**
    - 提供 500 MB 的純文字資料庫空間。由於不存影片和圖片（改存 Bunny 和外連圖床），500MB 足夠寫入數萬名會員的登入資料、購買紀錄與課程清單。
    - **限制：** 一週內若完全沒有流量，資料庫會自動休眠（下一個學員登入需要等 15 秒喚醒）。
    - **省錢除錯對策：** 網站上線後，寫一個定時排程（Cron Job），每 3 天自動連線查詢一次資料庫，就能永久保持喚醒狀態。
    - **必備安全機制：** 建立一個 GitHub Actions 的自動排程任務，每天備份資料庫並上傳到 Google Drive。

### 4. 系統自動寄信：Resend (免費版)

- **年費： NT$ 0** (Free Tier)
- **流量與限制：**
    - 每月免費 3,000 封信（每日上限 100 封）。
    - 這對於發送「註冊歡迎信」、「忘記密碼驗證」和「PayUni 購買成功收據」等系統觸發信來說綽綽有餘，在擁有前 1,000 名活躍會員前，完全不需付費。

### 5. 網域 (Domain) 與金流 (PayUni)

- **網域年費： NT$ 0** (直接沿用您現有的 `fu-notes.com`，免費切出 `bds` 子網域指向 Netlify)。
- **金流月租： NT$ 0** (PayUni 免收系統月租費)。
- **限制：** 無固定成本。僅在學員刷卡成功時，從營收中自動扣除 2.x% ~ 3% 的手續費。沒成交就完全不收費。

## 三、 總花費精算總結 (極致成本版)

- **每月固定總花費： 約 NT$ 100 元** (僅有 Bunny 的變動流量與儲存費)
- **每年營運總花費： 約 NT$ 1,200 元有找**



開發守則
1.關鍵第三方 API 串接邏輯 (商用級別要求)

1.1 PayUni 金流串接

發起交易： 將訂單資訊、商店代號 (MerchantID) 與 HashKey/HashIV 進行 SHA256 加密，組裝成 HTML Form POST 給 PayUni。

Webhook 接收 (/api/webhook/payuni)： 1. 接收 PayUni 的非同步付款通知 (Return URL / Notify URL)。
嚴格驗證 回傳的 Hash 值是否正確 (防偽造)。
若付款成功，將資料庫 orders 表的狀態更新為 paid。

1.2 Bunny.net 影片防盜 Token 演算法

在 Next.js Server Actions 或 API Route 中執行（絕不可在前端暴露 Bunny Key）。

演算法所需參數：SecurityKey (來自 Bunny 控制台), VideoID, ExpirationTime (目前時間 + 6小時), UserIP (可選，綁定學員 IP)。

使用 SHA256 進行 Hash 計算，組合出最終的 HLS 播放網址：https://video.yourdomain.com/{VideoID}/playlist.m3u8?token={HashToken}&expires={ExpirationTime}

2. 嚴格執行原則
錯誤處理 (Error Handling)： 作為商用系統，所有 API 呼叫、資料庫寫入都必須包含 try-catch，並在前端顯示友善的 Toast 錯誤訊息（例如「付款連線逾時，請重試」）。

環境變數安全 (Env Security)： 所有機密金鑰 (PayUni HashKey, Bunny Security Key, Supabase Service Role Key) 必須且只能存在 .env.local，並且只能由 Server 端存取。